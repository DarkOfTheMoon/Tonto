program run_atom

   use TYPES
   use TEXTFILE
   use ATOM

#include "macros"

   implicit none

   TEXTFILE, PTR :: in,out
   ATOM :: a

   in.create("input")
   in.open_for_read
   stdin => in

   out.create("output")
   out.open_for_write
   stdout => out

   a.read
   a.put

end 
