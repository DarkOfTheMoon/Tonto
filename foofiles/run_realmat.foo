program run_mat

! $Id$

  use TYPES
  use VEC
  use MAT
  use TEXTFILE

# include "macros"

  implicit none
  
  MAT, PTR :: matrix
  MAT, PTR :: eigenvectors
  VEC, PTR :: eigenvalues
  TEXTFILE, PTR :: in, out

  matrix.create(2,2)
  eigenvectors.create(2,2)
  eigenvalues.create(2)

  matrix = ONE

! matrix.solve_eigenproblem(eigenvalues,eigenvectors)
  call solve_eigenproblem_(matrix,eigenvalues,eigenvectors)

  write(*,*) eigenvalues

end 

