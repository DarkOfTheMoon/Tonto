program run_basis

   use TYPES
   use SYSTEM
   use SHELL
   use SHELLVEC
   use BASIS
   use IMAT
   use INPUT
   use OUTPUT

#include "macros"

   implicit none

   INPUT, PTR :: in
   OUTPUT, PTR :: out
   SHELLVEC, PTR :: sh
   BASIS :: b

   out.create("output")
   out.open
   in.create("input")
   in.open

   b.read(in)
   b.put(out)

end
