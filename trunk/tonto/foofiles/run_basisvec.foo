program run_basisvec

   use TYPES
   use SYSTEM
   use SHELL
   use SHELLVEC
   use BASIS
   use BASISVEC
   use IMAT
   use INPUT
   use OUTPUT

#include "macros"

   implicit none

   INPUT, PTR :: in
   OUTPUT, PTR :: out
   SHELLVEC, PTR :: sh
   BASISVEC, PTR :: b

   out.create("output")
   out.open
   in.create("input")
   in.open

   b.read(in)
   b.put(out)

end
