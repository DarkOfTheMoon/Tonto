program run_crystal

! $Id$

   use TYPES
   use SYSTEM
   use TEXTFILE
   use CRYSTAL

   implicit none

   c :: CRYSTAL

   tonto.initialize

   stdin.create_stdin
   stdin.open

   stdout.create_stdout
   stdout.open

   c.read_keywords

end
