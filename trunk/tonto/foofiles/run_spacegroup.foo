program run_spacegroup

! $Id$

   use TYPES
   use SYSTEM
   use TEXTFILE
   use SPACEGROUP

   implicit none

   sg :: SPACEGROUP

   tonto.initialize

   stdin.create_stdin
   stdin.open
   stdout.create_stdout
   stdout.open

   sg.read_keywords

end
