program run_shell
! $Id$

   use TYPES
   use SYSTEM
   use TEXTFILE
   use SHELL

#include "macros"

   implicit none

   TEXTFILE* :: in,out
   SHELL* :: sh

   tonto.initialize

   in.create_stdin
   in.open_for_read

   out.create_stdout
   out.open_for_write

   sh.create 
   sh.read
   sh.put

end 
